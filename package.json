{
  "name": "ecommerce-pro",
  "version": "1.0.0",
  "description": "Full-stack E-Commerce Pro Platform",
  "main": "api/index.js",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && cd ../api && npm install --legacy-peer-deps",
    "seed": "cd api && npm run seed",
    "server": "cd api && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "npx concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install --legacy-peer-deps && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
