{
  "name": "ecommerce-pro",
  "version": "1.0.0",
  "description": "Full-stack E-Commerce Pro Platform",
  "main": "server/server.js",
  "scripts": {
    "install-all": "npm install --legacy-peer-deps && cd client && npm install --legacy-peer-deps && cd ../server && npm install --legacy-peer-deps",
    "seed": "cd server && npm run seed",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "dev": "npx concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install --legacy-peer-deps && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "cloudinary": "^1.41.3",
    "multer-storage-cloudinary": "^4.0.0"
  }
}
